{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Start Container",
      "type": "shell",
      "command": "docker-compose up --build -d",
      "options": {
        "cwd": "${workspaceFolder}/.devcontainer"
      },
      "problemMatcher": [],
      "detail": "Builds the image if needed and starts the container in the background."
    },
    {
      "label": "Start Container & Open Shell",
      "dependsOn": ["Build and Start Container"],
      "dependsOrder": "sequence",
      "type": "shell",
      "command": "docker exec -it isaac-lab-dev-container bash",
      "options": {
        "cwd": "${workspaceFolder}/.devcontainer"
      },
      "problemMatcher": [],
      "detail": "This is the main task. It ensures the container is running, then opens an interactive shell inside it."
    }
  ]
}